-- 테스트 데이터 지우기
--
-- Supabase SQL Editor에 붙여 넣습니다. **스키마는 그대로 두고 내용만 비웁니다.**
--
-- ⚠️ 행사 당일 아침에 반드시 한 번 실행하세요. 테스트로 쌓인 카드가 남아 있으면
--    진열장 30칸을 이미 차지하고 있어 실제 참가자가 SHOWCASE_FULL로 튕깁니다.

begin;

-- entries는 participants를 참조하므로 cascade 하나로 둘 다 비워집니다.
truncate participants cascade;

-- 화면 상태는 지우지 않고 초기값으로 되돌립니다 (행이 하나여야 하므로).
update show_state
   set layout = 'LIVE', qr_visible = true, shelf_page = 0, updated_at = now();

commit;

-- ---------------------------------------------------------------------------
-- 이것만으로 지워지지 않는 것
-- ---------------------------------------------------------------------------
--
-- ① Supabase Storage의 증서 이미지
--    DB 행만 지워지고 파일은 버킷에 남습니다. 대시보드에서 버킷을 비우세요.
--    참조가 사라졌으므로 화면에는 안 나옵니다.
--
-- ② IPFS에 핀된 것
--    **되돌릴 수 없습니다.** 테스트로 핀한 증서는 CID가 살아 있습니다.
--    Pinata에서 unpin해도 다른 노드가 갖고 있을 수 있습니다.
--
-- ③ 체인에 발행된 증서  ← 제일 중요합니다
--    **되돌릴 수 없고, 그 지갑 주소는 영구히 사용 불가입니다.**
--    컨트랙트의 hasBeenIssued(address)는 소각한 뒤에도 true로 남습니다.
--
--    그래서 **테스트 민팅은 매번 새 주소로 하세요.** 본인 지갑이나 운영진 지갑으로
--    테스트하면 행사 당일 그 사람은 증서를 받지 못합니다.
--
--    현재 발행 상태는 nextTokenId()로 확인합니다. 스모크 테스트로 1~4가 쓰였고
--    (contracts/FUJI_SMOKE_TEST.md), 그 뒤 테스트 민팅만큼 늘어납니다.
