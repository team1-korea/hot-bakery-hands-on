# 시스템 설계

행사 시스템의 역할 경계와 운영 정책입니다. **엔드포인트 명세는 여기 없습니다** —
그건 [API_REFERENCE.md](./API_REFERENCE.md)입니다.

> 이 파일은 `API.md`였습니다. 이름이 엔드포인트 문서처럼 읽혀
> [API_REFERENCE.md](./API_REFERENCE.md)와 헷갈리기에 바꿨습니다.

| 찾는 것 | 문서 |
|---|---|
| 엔드포인트, 요청·응답, 오류 코드, 환경변수 | [API_REFERENCE.md](./API_REFERENCE.md) |
| 상태 머신, 민팅 순서, 구현 체크리스트 | [PIPELINE.md](./PIPELINE.md) |
| 확정된 결정과 그 근거 | [DECISIONS.md](./DECISIONS.md) |
| 컨트랙트 호출 규약 | [contracts/INTEGRATION_GUIDE.md](./contracts/INTEGRATION_GUIDE.md) |

`[결정 필요]`로 남아 있던 항목은 [DECISIONS.md](./DECISIONS.md)에서 확정했습니다. 이 문서 서두의
안내("정하는 사람이 여기를 고쳐 주세요")에 따른 것입니다.

화면 응답 모델(`Entry`, `ShowState`)의 정본은
[`apps/web/lib/api/types.ts`](./apps/web/lib/api/types.ts)입니다. 문서와 어긋나면 타입이 맞습니다.

---

## 1. 역할 경계

**프론트엔드가 하는 것**

- **Privy Google 로그인.** 참가자 계정에 embedded EVM 지갑을 생성합니다.
- Privy access token을 참가자 API의 `Authorization: Bearer ...` 헤더에 붙입니다.
- 참가자가 사진을 끌고 확대해 정사각 틀에 맞추면 그대로 잘라냅니다(최대 1080px JPEG,
  원본 3~5MB를 200KB 안팎으로). `PhotoCropper.tsx`와 `lib/photo.ts`.
- **증서 이미지 합성.** 같은 브라우저 캔버스에서 정사각형 쿠키 사진을 1080×1440 세로형 프레임에
  합성합니다.
  **업로드하는 것은 합성본 한 장**이고 원본은 서버로 보내지 않습니다.
- **로그인·닉네임 시점의 등록 요청.** 사진을 찍기 전에 `POST /api/participants`를 부릅니다.
- 닉네임·사진 입력, 제출 확인 화면. **닉네임이 사진보다 먼저입니다.**
- 운영자 화면의 **대리 업로드에서도 같은 합성을 씁니다.** 합성 함수를 참가자 화면 전용으로
  만들지 마세요.
- 행사장 TV: 접수 → 오븐 → 진열장 카드 이동, 진열장 쪽 넘기기, 참가 QR.
- 운영자 화면: 참가자 상태 확인, 카드 내리기, 실패 재시도, 앞 화면 전환.
- `GET /api/state`를 1초마다 폴링합니다.

**백엔드가 하는 것**

- **Privy 인증 검증과 주소 결정.** 토큰을 검증하고, 검증된 사용자의 linked accounts에서 embedded EVM
  지갑 주소를 얻어 저장합니다. 요청 본문으로 온 주소를 신뢰하지 않습니다.
- **컨트랙트 호출.** 발행 트랜잭션은 사용자 지갑이 아니라 `MINTER_ROLE`을 가진 서버 민터가 서명합니다.
  민터 개인키는 서버에만 있습니다. 프론트에는 지갑 UI도, viem도 없습니다.
- **메타데이터 JSON 생성과 IPFS 업로드.**
- **운영 기간의 증서 이미지와 상태 저장.**

- **받은 합성본을 그대로 IPFS에 핀.** **서버는 이미지를 다시 그리지 않습니다** — 재인코딩하면
  참가자가 확인 화면에서 본 증서와 체인에 박히는 증서가 달라집니다. `sharp`·`node-canvas`·
  `satori` 어느 것도 필요 없습니다.

**아무도 하지 않는 것**

- **참가자 개인키 생성·보관.** Privy가 사용자 Google 계정과 연결해 관리합니다. 프론트도 백엔드도
  참가자 개인키를 조회하거나 저장하지 않습니다.

개발 기본 컨트랙트는 Fuji에 배포돼 있습니다:
`0x67Ce0bb25ee58B6D000d209B051b9E846D0d6b36`. 행사 운영은 메인넷 컨트랙트를 배포한 뒤
체인 ID·주소·배포 블록을 환경변수로 함께 전환합니다.

---

## 2. 인증과 오리진

참가자 API는 Privy Bearer token을 씁니다. `credentials: 'include'`는 **운영자 세션 쿠키에만** 필요합니다.

**백엔드를 같은 오리진에 두면**(Next 라우트를 그대로 대체) 추가 설정이 없습니다.

**다른 오리진에 두면**(예: `api.example.com`) 이게 필요합니다:

- `Access-Control-Allow-Origin`에 프론트 오리진을 **정확히** 지정(`*`는 운영자 쿠키와 함께 못 씁니다)
- `Access-Control-Allow-Credentials: true`
- 운영자 세션 쿠키를 `SameSite=None; Secure`로
- `Authorization`, `Content-Type`, `PATCH`, `multipart` 프리플라이트 허용

---

## 3. 실행 모드

`app/api/` 라우트 계약은 하나이고 내부 구현만 환경에 따라 갈립니다.

| 조건 | 저장소·사진 | 파이프라인 | 인증 |
|---|---|---|---|
| 로컬, `DATABASE_URL` 없음 | `store.memory.ts` + 메모리 사진 | `setTimeout` 목 상태 전이, 가짜 tx | Privy 서버 변수 모두 없으면 개발용 목 신원 |
| 실제, `DATABASE_URL` 있음 | `store.pg.ts` + Supabase Storage | `after()` → Pinata → 설정된 C-Chain 민팅 | Privy access token 검증 + Users API 지갑 조회 |

부분 설정은 안전하게 실패합니다. 운영 환경이거나 Privy 변수 일부만 있으면 목 인증으로 열리지 않고
`401`이며, 실제 DB 파이프라인에 Pinata·민터 설정이 빠지면 해당 항목은 `FAILED`로 기록됩니다.

메모리 모드는 화면 개발용입니다. 재시작하면 데이터가 사라지고, `MOCK_FAILURE_RATE`로 실패를
만들며, 실제 IPFS·체인·동시성은 검증하지 않습니다. 운영 배포는 환경변수 전체를 넣고
`/api/internal/sweep` Cron까지 설정해야 합니다.

---

## 4. 개인정보와 서비스 수명

백엔드가 저장하는 것은 **Privy DID**, **참가자 지갑 주소**, **닉네임**, **프레임을 두른 합성
증서**입니다. **원본 사진은 서버로 오지 않습니다** — 프론트가 합성해서 보내므로 파기할 원본이
애초에 없습니다.
Google 계정과 참가자 개인키는 Privy가 관리하며, 백엔드는 이메일이나 개인키를 지속 저장하지 않습니다.

실명은 받지 않지만 닉네임과 합성 증서는 공개 IPFS에 올라갑니다. 닉네임도 식별 가능성이 있는
공개 정보로 안내해야 합니다.

운영용 DB와 사진 저장소, 프론트엔드와 API는 **행사 종료 30일 후에 내립니다.**

IPFS에 올라가는 것은 **프레임을 두른 합성 증서와 닉네임이 포함된 메타데이터**입니다. 쿠키 사진도
**한 번 올라가면 사실상 지울 수 없습니다.** 지갑 주소·거래 기록은 블록체인에 공개됩니다.
원본 사진은 참가자 폰에만 남습니다.

제출 화면에서 이 보관 범위와 서비스 종료일을 알려야 합니다.

서비스가 종료된 뒤에는 증서를 컨트랙트의 `tokenURI`와 IPFS CID로 조회합니다. 참가자 지갑이 Privy
소유이므로 **증서 자체는 참가자에게 남습니다.**

---

## 5. 남은 위험

- **`GET /api/state`는 인증이 없습니다.** Privy DID·Google 계정 정보·지갑 주소·`failureReason`을
  넣지 마세요. TV URL을 아는 사람은 누구나 봅니다.
- **운영자 비밀번호에 시도 횟수 제한이 없습니다.** 짧은 값을 공개 주소에 두면 무작위 대입에 뚫립니다.
  **대리 업로드가 생기면서 위험이 커졌습니다** — 운영자 화면을 잡으면 아무 참가자에게 아무 사진이나
  붙일 수 있습니다. 비밀번호를 길게 잡으세요.
- **운영자 쿠키에 `Secure` 플래그가 없습니다.** 행사장에서 노트북의 HTTP LAN 주소로 열 수 있어야 하기
  때문입니다. HTTPS 배포에서는 붙이는 것이 낫습니다.
- **확정 NFT 디자인 원본은 Git에 없습니다.** 운영 배포의
  `NEXT_PUBLIC_CERTIFICATE_FRAME_URL`이 CORS를 허용하는 외부 에셋을 가리키지 않으면 참가자와
  운영자 대리 업로드 모두 증서를 합성할 수 없습니다.
