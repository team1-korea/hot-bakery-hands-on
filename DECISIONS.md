# 결정 기록

확정된 결정과 그 근거입니다. 되돌리려면 여기에 근거를 적고 담당자에게 확인받으세요.

`ARCHITECTURE.md`(옛 `API.md`)가 `[결정 필요]`로 남겨 둔 항목들을 여기서 확정했습니다. 그 문서 서두의 안내
("정하는 사람이 여기를 고쳐 주세요")에 따른 것입니다.

---

## 확정

### 2026-08-19 · 참가자 인증은 Privy — 백엔드

프론트에서 Privy 구글 로그인을 처리하고, 백엔드는 `Authorization: Bearer <Privy 토큰>`을 검증합니다.

**근거.** 지갑 생성·이메일 발송·인증코드 만료 정책을 한 번에 해결합니다. 직접 구현하면 코드 발급,
메일 발송(SPF/DKIM, 스팸함 대응), 만료·재시도 제한을 전부 만들어야 하는데 행사 당일 인증 메일이
스팸함에 들어가면 진행이 멈춥니다.

**영향.** `POST /api/auth/request-code`, `POST /api/auth/verify`, `GET /api/auth/session` 세 개가
사라집니다. 프론트의 `EmailStep`·`CodeStep`과 `INVALID_EMAIL`·`INVALID_CODE` 오류 코드는
제거됐습니다.

### 2026-08-19 · 참가자 지갑은 Privy 임베디드 EOA — 백엔드

서버 커스터디와 HD 시드 파생을 검토했으나 채택하지 않았습니다.

**근거.** 시드 관리와 키 보관 부담이 사라집니다. 그리고 **참가자가 증서를 실제로 소유**하게 되어,
서버가 내려간 뒤에도 증서가 남습니다. 서버 커스터디에서는 참가자가 자기 지갑을 몰라 행사 후
증서에 접근할 방법이 없었습니다.

**주의.** 지갑 주소는 **서버가 Privy API로 조회**합니다. 클라이언트가 보낸 주소를 그대로 쓰면
임의의 주소로 민팅시킬 수 있습니다.

### 2026-08-19 · 민팅은 건별 즉시, 직렬 처리 — 백엔드

`batchMint`(최대 50)를 쓰지 않습니다.

**근거.**
1. `batchMint`는 원자적입니다. 한 명이라도 잘못되면 전원 발급이 취소됩니다.
2. 배치로 처리하면 모두 동시에 `MINTED`가 되어, TV의 오븐이 굽는 곳이 아니라 개찰구가 됩니다.
   건별 민팅은 영수증 대기 시간(2~5초)이 그대로 굽는 시간이 됩니다.
3. 처리량이 필요 없습니다. TV 애니메이션이 병목입니다(카드 하나 착지에 최소 3.4초).

**주의.** 민터 지갑이 하나라 동시 전송하면 nonce가 충돌합니다. DB 락으로 직렬화합니다.

### 2026-08-19 · 증서 이미지를 합성함 — 디자인 ~~(2026-08-20 개정)~~

쿠키 사진 바깥에 **프레임을 둘러 합성한 이미지**를 증서로 씁니다. 프레임은 디자인에서 제작합니다.

**IPFS에 올라가는 것은 합성본입니다.**

> ⚠️ **합성 주체가 바뀌었습니다.** 이 결정은 백엔드가 합성하는 것을 전제로 썼습니다.
> 2026-08-20에 **프론트가 합성**하는 것으로 바뀌었고, 원본 사진은 서버로 오지 않습니다.
> 아래 「2026-08-20 · 증서 합성을 프론트로 옮김」을 보세요. 프레임을 둘러 합성한다는 것 자체는
> 그대로입니다.

### 2026-08-19 · 실명을 받지 않음, 닉네임만 — 백엔드

**근거.** 메타데이터는 영구 공개이고 컨트랙트에 수정 함수가 없습니다. 닉네임 + 쿠키 사진이면
메타데이터에 개인정보가 들어가지 않아, 원본 사진과 이메일을 행사 후 파기할 수 있습니다.

**남은 일.** 프론트 문구가 아직 "이름"이라 실명을 유도합니다. `JoinSteps.tsx`의 7곳과
`JoinShell.tsx`의 진행 표시를 "닉네임"으로 바꾸고, "실명 대신 별명을 적어 주세요" 안내를
넣어야 합니다.

### 2026-08-19 · 메타데이터에 `external_url`과 `tokenId`를 넣지 않음 — 백엔드

```jsonc
{
  "name": "Avalanche Bakery 참가 증서 — 쿠키왕",
  "description": "...",
  "image": "ipfs://<CID>",
  "attributes": [ /* 닉네임, 행사, 발행일 */ ]
}
```

**근거 (`external_url`).** 행사 후 사라질 서버 주소를 체인에 영구히 박게 됩니다. 404를 가리키는
영구 링크보다 없는 편이 낫습니다. OpenSea 표준의 선택 필드라 없어도 유효합니다.
빈 문자열이 아니라 **키 자체를 뺍니다.**

**근거 (`tokenId`).** `mint(recipient, metadataURI)`는 메타데이터 URI를 인자로 받으므로 JSON이
민팅 **전에** IPFS에 올라가야 합니다. 그런데 `tokenId`는 민팅이 끝나야 알 수 있습니다.
`name`에 닉네임을 쓰면 이 순환이 없어집니다.

### 2026-08-19 · `image`는 `ipfs://` URI — 백엔드

게이트웨이 HTTPS 주소를 쓰지 않습니다.

**근거.** 컨트랙트에 URI 수정 함수가 없어 포인터는 어차피 불변입니다. HTTPS를 쓰면 그 도메인이
죽을 때 증서가 통째로 깨집니다. `ipfs://` + CID는 게이트웨이가 무엇이든 살아남습니다.

### 2026-08-19 · 실행 환경은 Vercel + Supabase + Pinata — 백엔드

Railway를 검토했으나 채택하지 않았습니다.

**근거.** Next.js `app/api/`가 그대로 백엔드라 배포 대상이 하나입니다. Railway는 무료 티어가
없고 배포 대상이 늘어납니다. 상시 프로세스가 필요한 이유는 민팅 직렬화뿐인데, 그건 Postgres
락으로 해결됩니다.

**주의.** 서버리스라 파이프라인을 `after()`로 돌려야 합니다. 목 구현의 `setTimeout` 방식은
응답 후 함수가 얼어붙어 동작하지 않습니다.

### 2026-08-19 · 개발 네트워크는 Fuji — 백엔드

`43113` / `0x67Ce0bb25ee58B6D000d209B051b9E846D0d6b36`. 메인넷 배포는 **아직 없습니다.**

**주의.** 프론트의 익스플로러 링크가 C-Chain 메인넷으로 하드코딩돼 있고
(`components/display/Showcase.tsx`), 어디에도 `chainId`가 없습니다. Fuji에서 실제 `txHash`가
생기면 링크가 열리지 않습니다. 운영 전환 전에 프론트에 체인 설정을 넣어야 합니다.

---

### 2026-08-19 · 서비스는 행사 종료 30일 후 종료, 데이터 파기 — 프론트

운영용 DB·사진 저장소·프론트엔드·API를 행사 종료 30일 후에 내립니다. Supabase의 Privy DID·지갑
연결 정보·사진을 파기하고 IPFS CID만 백업합니다.

**근거.** 임시 행사 서비스이고, 메타데이터에 개인정보가 없어 파기해도 증서가 남습니다. 참가자
지갑이 Privy 소유라 서비스 종료 후에도 증서는 참가자에게 남고, 조회는 `tokenURI`와 IPFS CID로
합니다.

**남은 일.** 제출 화면에서 보관 범위와 서비스 종료일을 알려야 합니다. 지금은
"증서는 공개 기록으로 남아요"까지만 말합니다.

### 2026-08-19 · `WALLET_NOT_FOUND` 오류 코드 추가 — 프론트

Privy 사용자에게 embedded EVM 지갑이 없을 때 `400 WALLET_NOT_FOUND`를 돌려줍니다.

**반영 완료.** `types.ts`에 `WALLET_NOT_FOUND`가 있고 폐기된 이메일 인증 오류 코드는 없습니다.

### 2026-08-20 · 증서 합성을 프론트로 옮김 — 프론트

프레임 합성을 **참가자 브라우저에서** 합니다. 서버는 합성하지 않습니다.

리사이즈와 합성을 같은 캔버스에서 끝내고, **합성본 한 장만** 업로드합니다. 원본 사진은 서버로
오지 않습니다. `apps/web/lib/photo.ts`의 `cropPhoto()`가 이미 정사각형으로 잘라 최대 1080px JPEG로
내보내고 있으므로, **그 출력에 프레임을 얹습니다.**

> 프레임을 두르면 결과 이미지가 정사각형보다 커집니다. 캔버스 규격을 디자인에서 받을 때
> **프레임 포함 최종 크기**를 함께 정하세요. `PhotoCropper`의 정사각 틀은 사진 영역이지
> 증서 전체가 아닙니다.

**근거.**
1. 참가자가 **제출 전에 최종 증서를 봅니다.** 서버 합성이면 자기 증서를 처음 보는 곳이 TV입니다.
2. 서버에 이미지 처리 의존성(`sharp`·`node-canvas`·`satori`)이 사라집니다. 서버리스에서 이런
   네이티브 의존성은 콜드 스타트와 번들 크기 문제를 만듭니다.
3. **파기할 원본이 없습니다.** 원본을 아예 받지 않는 것이 "받아서 나중에 파기한다"보다 강합니다.

**영향.**
- `POST /api/entries`가 받는 이미지는 **합성본 하나**입니다. 파일이 둘이 아닙니다.
- 서버는 받은 바이트를 **재인코딩하지 않고 그대로** 핀합니다. 다시 그리면 참가자가 확인 화면에서
  본 증서와 체인에 박히는 증서가 달라집니다.
- `Entry.photoUrl`에 합성본이 들어갑니다. 중복이던 `certificateUrl`은 타입과 화면에서 제거했습니다.
- **`EntryStatus`의 `RENDERED`가 사라집니다.** 서버에 합성 단계가 없습니다. 상태 목록에 남겨두면
  "서버가 렌더한다"는 오해를 계속 만듭니다.
- 합성 함수는 **참가자 화면 전용으로 만들지 마세요.** 운영자 대리 업로드에서 같은 합성이
  필요합니다(아래 결정 참고).

**선행 조건(완료).** 프레임 에셋과 합성 규격(캔버스 크기, 사진 위치, 닉네임·증서번호를 넣을지)을
**프론트에** 전달받아 아래 최종 시안 결정에 반영했습니다.

### 2026-08-24 · 최종 증서는 1080×1440 세로형 — 프론트·디자인

참가자가 조정하는 쿠키 사진은 기존처럼 정사각형으로 자르되, 프레임을 포함한 최종 NFT 이미지는
**1080×1440px(3:4)**로 합성합니다. `CERTIFICATE ID`는 이미지에 넣지 않고 닉네임만 디자인에서
정한 위치와 글꼴로 프론트가 그립니다.

**근거.** 확정 중인 증서 시안이 3:4 세로형이고, 제목·사진·닉네임·행사 정보를 위에서 아래로
읽는 구성을 정사각형으로 압축하면 시안의 위계가 무너집니다.

**영향.** 참가자 미리보기와 완료 화면은 3:4로 표시하고, TV 카드와 운영자 썸네일은 `cover`로
자르지 않습니다.

### 2026-08-25 · 확정 시안과 닉네임 서체 — 프론트·디자인

확정 시안은 960×1280 JPG입니다. 원본은 GitHub에 올리지 않고 운영 배포의
`NEXT_PUBLIC_CERTIFICATE_FRAME_URL`이 가리키는 외부 저장소에서 불러옵니다. 브라우저는 이를
1080×1440 캔버스에 맞춰 그리고, 정사각형 사진과 닉네임을 더해 최종 JPEG를 만듭니다. 로컬에서
같은 파일을 쓸 때는 Git에서 제외된 `apps/web/public/assets/certificate/nft-design.jpg`에 둡니다.

- 사진 안내선은 원본 기준 `x=297…661`, `y=528…892`입니다. 사진은 최종 캔버스에서 안내선보다
  사방 약 5px 크게 배치해 빨간 선과 JPEG 번짐을 완전히 덮습니다. 별도 테두리는 그리지 않습니다.
- 닉네임은 Pretendard Bold(700), 자간 0, `#DA242D`로 표시합니다. 12자까지 이름 영역 안에서
  자동으로 크기를 줄입니다.
- `CERTIFICATE ID`는 넣지 않습니다.
- Pretendard Bold는 민팅 시 외부 네트워크에 의존하지 않고 모든 한글 닉네임을 같은 서체로
  합성하도록 전체 WOFF2를 함께 배포합니다.

### 2026-08-20 · 로그인·닉네임 시점에 백엔드 등록 — 프론트·백엔드

참가자가 Privy 구글 로그인을 마치고 닉네임을 넣으면, **사진을 찍기 전에** 백엔드에 등록합니다.
이때 행이 만들어지고 **TV 작업대에 카드가 올라갑니다.**

**근거 — 사진을 못 올리는 참가자를 구제할 수 있습니다.**
행사는 한 번뿐이고 14명 중 1명이 못 받으면 그 사람에게는 100% 실패입니다. 제출 시점에 행을
만들면, 업로드가 계속 실패하는 참가자는 **서버에 존재조차 하지 않아** 운영자가 도울 방법이
없습니다. 등록을 앞당기면 지갑 주소와 닉네임이 이미 있으므로, 사진을 어떤 경로로든 받아
운영자가 대신 올려 파이프라인을 정상으로 돌릴 수 있습니다.

**부수 이득.**
- `WALLET_NOT_FOUND`를 **사진 찍기 전에** 잡습니다. 지금 계약대로면 참가자가 사진을 다 찍고
  제출을 누른 뒤에야 지갑이 없다는 것을 압니다.
- 운영자가 **미제출자를 봅니다.** 지금은 제출 안 한 사람이 서버에 없어 명단 대조가 안 됩니다.
- 참가자들이 사진을 찍고 고르는 몇 분 동안 TV가 비지 않습니다.

**영향.**
- 프론트의 이메일+코드 인증을 **Privy 구글 로그인으로 교체**해야 합니다. `EmailStep`·`CodeStep`
  제거는 2026-08-19 결정에 이미 있었고, 여기서 **닉네임 입력을 사진보다 앞으로** 옮기는 것이
  추가됩니다(`JoinFlow.tsx`의 `Stage`가 현재 `photo → nickname` 순서입니다).
- `EntryStatus`에 **`JOINED`**(등록됨, 사진 없음)가 생깁니다.
- **`shelfIndex`는 등록 시점이 아니라 사진 제출 시점에 배정합니다.** 등록 때 배정하면 이탈자가
  진열장 칸을 영구히 점유해 격자에 구멍이 남습니다(`Showcase.tsx`가 `shelfIndex`로 칸을 찾습니다).
  제출 때 배정하면 `shelf_index < 30` 제약이 정원 역할을 그대로 하면서 이탈자를 세지 않습니다.
- `ALREADY_SUBMITTED`는 행의 존재가 아니라 **사진 유무**로 판정합니다.
- **이탈자 스위퍼가 필요합니다.** 오래 `JOINED`로 멈춘 행을 자동으로 `hidden` 처리하지 않으면
  로그인만 하고 사라진 사람의 카드가 행사 내내 작업대에 남습니다. 자동으로 숨긴 참가자가
  늦게 사진을 제출하면 다시 표시하되, 운영자가 직접 숨긴 카드는 그대로 둡니다.

**주의.** `GET /api/state`는 인증이 없습니다. 등록 시점에 얻는 **지갑 주소와 DID가 응답에 섞이지
않도록** 하세요. 카드에 나가는 것은 닉네임뿐입니다.

### 2026-08-20 · TV 세 구역은 "누가 손대야 하는가"로 나눔 — 프론트·백엔드

| 구역 | 뜻 | 상태 |
|---|---|---|
| **오븐 대기**(작업대) | 사람 손이 필요하다 | `JOINED`, `FAILED` |
| **증서 오븐** | 기계가 처리 중이다 | `SUBMITTED`, `PINNED`, `MINTING` |
| **오늘의 진열장** | 끝났다 | `MINTED` |

```
JOINED ──→ SUBMITTED ──→ PINNED ──→ MINTING ──→ MINTED
   └───────────┴────────────┴──────────┴──────→ FAILED
```

**근거.** 프론트가 합성을 가져가면서 서버의 제출 후 작업이 IPFS 핀 두 번으로 줄었습니다. 제출을
경계로 잡지 않으면 작업대는 카드가 몇 초 스쳐가는 통과 구역이 되고, 정작 참가자들이 사진을 찍는
몇 분 동안 비어 있습니다. 제출을 경계로 두면 **작업대에 남는 것이 전부 운영자가 볼 것**(사진을
아직 안 낸 사람, 실패한 사람)이 되어 구역마다 할 일이 명확해집니다.

**영향.** `displaySequence.ts`의 `zone()`을 바꿔야 합니다. 이동 큐·오븐 슬롯·직렬 이동 기계는
**건드리지 않습니다.**

```ts
function zone(status: EntryStatus) {
  if (status === 'MINTED') return 'shelf';
  if (status === 'JOINED' || status === 'FAILED') return 'workbench';
  return 'oven';   // SUBMITTED · PINNED · MINTING
}
```

`SUBMITTED`와 `PINNED`는 TV에서 구별되지 않습니다. 실패 지점을 진단하기 위해 남깁니다.

**잃는 것.** 오븐이 IPFS와 체인을 함께 삼키므로 "오븐 = 블록체인"이라는 설명선이 흐려집니다.
대신 "작업대 = 사람이 준비하는 곳, 오븐 = 서버가 처리하는 곳"이 생깁니다. IPFS와 체인의 구분은
교육 세션에서 합니다.

### 2026-08-20 · 운영자 대리 사진 업로드 — 백엔드

운영자가 참가자를 대신해 사진을 올려 파이프라인을 시작할 수 있습니다. 위 등록 결정의 **핵심
목적**입니다.

**영향.**
- 운영자 API가 하나 늘어납니다. 지금은 `hidden`·`retry`·`show`뿐입니다.
- 합성이 브라우저에 있으므로 **운영자 화면에서도 같은 합성을 해야 합니다.** 운영자 화면도
  브라우저이므로 `lib/photo.ts`를 재사용합니다. 그래서 합성 함수를 공용으로 두어야 합니다.

**주의.** 이 기능이 생기면 **운영자는 아무 참가자에게 아무 사진이나 붙일 수 있습니다.** 운영자
비밀번호에 시도 횟수 제한이 없고 행사장에서 HTTP LAN 주소로 열립니다. **비밀번호를 길게
잡으세요.** 현재 로컬 값(`hotbakery`)은 짧습니다.

### 2026-08-25 · 행사 운영은 Avalanche C-Chain 메인넷으로 전환 — 백엔드

로컬 개발과 사전 검증의 기본 네트워크는 계속 Fuji(`43113`)를 사용하지만, 이번 행사 운영은
메인넷(`43114`) 컨트랙트를 새로 배포한 뒤 전환합니다.

메인넷의 `CERTIFICATE_ADDRESS`와 `CERTIFICATE_DEPLOYMENT_BLOCK`은 Fuji 리허설 중에도 Vercel에
미리 저장할 수 있습니다. Fuji는 이 두 값을 무시하고 커밋된 `deployments/43113.json`만 사용합니다.
메인넷 배포 결과는 `deployments/43114.json`에 주소·배포 트랜잭션·블록·관리자·민터를 공개 기록합니다.
초기 `MINTER_ROLE`은 현재 Production 서버 민터와 같은 주소에 부여하고 Vercel의 기존
`MINTER_PRIVATE_KEY`를 그대로 사용합니다. 배포 전 공개 계획에도 이 민터 주소를 고정해 다른 주소로
기록하지 못하게 합니다.
`chain:prepare-mainnet`은 이 기록을 온체인 코드와 역할에 대조한 뒤 주소·블록만 사전 등록하고 활성
체인은 건드리지 않습니다. 실제 활성화 스위치는 `NEXT_PUBLIC_CHAIN_ID`이며, 행사 때 `chain:switch`가
검증된 주소·블록을 먼저 맞춘 뒤 체인 ID를 마지막에 바꿉니다. 주소만 바꾸거나 배포 블록을 빠뜨리면
이벤트 복구가 잘못된 구간을 조회할 수 있으므로 메인넷 선택 시에는 두 값을 모두 필수로 검증합니다.

이번 행사만 운영한 뒤 서비스를 종료하므로 커스텀 RPC는 추가하지 않습니다.
`AVALANCHE_RPC_URL`을 비워 선택한 체인의 공개 RPC를 사용합니다. 배포 전에는 메인넷 컨트랙트의
민터 권한과 서버 민터의 AVAX 잔액을 확인합니다.

---

## 아직 미정

| 항목 | 정할 사람 | 메모 |
|---|---|---|
| 증서 페이지(`/certificates/{tokenId}`) | 백엔드 | 미구현. `external_url`을 뺐으므로 필수는 아니지만, 운영 기간 조회용으로 검토 중 |
| 발행 완료 시 참가자에게 알릴지 | 팀 | Privy가 이메일을 갖고 있어 우리가 보낼 수단이 없습니다 |
| 오발급 정정 경로(`revoke` → `reissue`) | 백엔드 | 미구현. 행사 규모상 생략 가능 |
| 운영자 비밀번호 시도 횟수 제한 | 백엔드 | 현재 무제한 |
